matrix = [][] #a matrix

firstWire = list(map(str, input()))
secondWire = list(map(str, input()))

for direction in firstWire:
	if direction[0] == "R":
		pass
	elif direction[0] == "L":
		pass
	elif direction[0] == "U":
		#do something
	elif direction[0] == "D":
		#do something


for direction in secondWire:
	 for all directions:
	 	check if (there's anything marked '1'. if so, mark it as 'X'):



calculate manhatten distance between each intersection pt marked as 'X'
